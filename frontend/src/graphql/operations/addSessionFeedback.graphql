mutation AddSessionFeedback($input: AddSessionFeedbackInput!) {
  addSessionFeedback(input: $input) {
    session {
      id
      status
      feedback {
        rating
        comment
        createdAt
      }
      updatedAt
    }
  }
}
